# 이슈 생성 시 자동 안내 코멘트
name: Issue Created Auto Comment

on:
  issues:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `
                    👋 이슈 생성 감사합니다!
                    
                    ✅ 작업 시작 전 확인
                    - Assignee 지정
                    - 적절한 Label 선택
                  
                    📌 PR 생성 시 아래 문구를 꼭 추가해주세요:
                      \`\`\`
                      close #${context.issue.number}
                      \`\`\`
                      `
                    })
