# 이슈 생성 시 자동 안내 코멘트
name: Issue Created Auto Comment

on:
  issues:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '👋 @${{ github.event.issue.user.login }}님, 이슈를 생성해주셔서 감사합니다! 🙂\n\n이슈를 신속하게 처리할 수 있도록 최대한 자세한 정보를 제공해 주시기 바랍니다.'
            })
